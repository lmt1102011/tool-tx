name: Test t32

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-t32.yml'

jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - env:
          V: ${{ secrets.KS_T32 }}
        run: echo "label: random 32 byte nhan duoc ${#V} ky tu"
