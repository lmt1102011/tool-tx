name: Test t96

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-t96.yml'

jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - env:
          V: ${{ secrets.KS_T96 }}
        run: echo "label: random 96 byte nhan duoc ${#V} ky tu"
