name: Test Secret Two

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-secret2.yml'

jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - name: Ref secret khac
        env:
          V: ${{ secrets.KS_B64_TEST }}
        run: echo "gia tri nhan duoc = [$V]"
