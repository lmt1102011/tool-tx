name: Test Secret Big

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-secret-big.yml'

jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - name: Ref secret lon
        env:
          V: ${{ secrets.KS_B64_BIG }}
        run: echo "do dai nhan duoc = ${#V}"
