name: Test S512

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-s512.yml'

jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - env:
          V: ${{ secrets.KS_S512 }}
        run: echo "nhan duoc ${#V} ky tu"
