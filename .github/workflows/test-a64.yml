name: Test a64

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-a64.yml'

jobs:
  t:
    runs-on: ubuntu-latest
    steps:
      - env:
          V: ${{ secrets.KS_A64 }}
        run: echo "ascii 64 byte nhan duoc ${#V} ky tu"
